apply plugin: 'idea'
apply plugin: 'groovy'
apply plugin: 'maven'

version = '0.1-SNAPSHOT'
group = 'com.moowork.gradle'

dependencies {
    compile gradleApi()
}

task wrapper( type: Wrapper ) {
    gradleVersion = '1.6'
}
